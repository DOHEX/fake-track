[project]
name = "fake-track"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "DOHEX", email = "dohex@outlook.com" }
]
requires-python = ">=3.15"
dependencies = [
    "cryptography>=46.0.6",
    "rich>=13.9.4",
    "requests>=2.32.3",
    "typer>=0.24.1",
]

[project.scripts]
fake-track = "fake_track.cli:app"
fack-track = "fake_track.cli:app"

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"

[tool.ruff.lint]
select = ["I"]
